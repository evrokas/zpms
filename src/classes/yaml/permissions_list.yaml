- table: permissions_list
  class: permissionsListClass
  fields:
    - a: char(36) not null
    - b: char(36) not null


# this is the list that connects guid
# used to connect a guid from users, roles, permissions entries
# to b guid from users, roles, permissions entries
