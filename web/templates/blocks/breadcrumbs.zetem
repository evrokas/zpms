<div class="breadcrumbs">

{#
    {% $temp = implode(' \\/\\ ', $path) %}
    {% $path = explode('\\', $temp) %}
    <pre>
        {{print_r($path)}}
    </pre>
#}
    

    <ul>
        {% foreach($path as $element): %}
            <li {{attributes($element)}}>{{$element['text']}}</li>
        {% endforeach %}
    </ul>

</div>