{#
    parameters:
        region_name         name of the region
        blocks              array of blocks    
    
#}

<h2>Region: <span> {{ $region_name }}</span></h2>

{% foreach($blocks as $block): %}
<p>rendering block {{$block}}</p>
{{ $renderer->render("block.zetem") }}
{% endforeach %}
